//
//  Map3DViewContainer.swift
//  wowCastillaLeonTour
//
//  Created by Markel Juaristi on 3/9/24.
//



/*
import SwiftUI

struct Map3DViewContainer: View {
    @State private var selectedSpot: Spot?
    @StateObject private var viewModel = Map3DViewModel(appState: AppState())

    var body: some View {
        Map3DView(selectedSpot: $selectedSpot)
            .edgesIgnoringSafeArea(.all)
            .sheet(item: $selectedSpot) { spot in
                Map3DCallOutView(spot: spot, viewModel: viewModel)
                    .environmentObject(viewModel.appState)
            }
    }
}
*/
